const isAnagram = (test, original) => {
  // Loop though the test and the original
  original = Array.from(original);

  return;
};

console.log(isAnagram("foefet", "toffee"), "Should be true");
console.log(isAnagram("Buckethead", "DeathCubeK"), "Should be true");
console.log(isAnagram("Twoo", "WooT"), "Should be true");
console.log(isAnagram("dumble", "bumble"), "Should be false");
console.log(isAnagram("ound", "round"), "Should be false");
console.log(isAnagram("apple", "pale"), "Should be false");

// unfinished
